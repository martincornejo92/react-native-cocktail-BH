import { StyleSheet, View } from 'react-native';

export const listStyles = StyleSheet.create({
  header: {
    backgroundColor: '#FFF',
    color: '#000',
    fontSize: 20,
    fontWeight: 'bold',
    padding: 10
  },

  item: {
    color: '#000',
    fontSize: 16,
    padding: 12
  }
});
